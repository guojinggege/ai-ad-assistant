const { openAdminPage, closeBrowser, authExists, ADMIN_URL, HEADLESS } = require('../scripts/_browser');

module.exports = {
  openAdminPage,
  closeBrowser,
  authExists,
  ADMIN_URL,
  HEADLESS,
};
